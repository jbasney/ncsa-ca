# define global variables

# sanity check environment

_arch=`${GLOBUS_INSTALL_PATH}/sbin/config.guess`

if [ -z "${_arch}" ]; then
  echo "ERROR: could not determine current architecture" 1>&2
  exit 1
fi

# define Subroutines

# define defaults

# Parse commandline options

# Install the NCSA CA certificate and policy into the certificates
# directories.

# Potential places to install
_targets="/etc/grid-security/certificates ${GLOBUS_INSTALL_PATH}/share/certificates"

# Install ncsa-cert-request and ncsa-cert-retrieve scripts

# Modify security configuration files to use NCSA CA

# Done
