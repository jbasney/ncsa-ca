#!/bin/sh
#
# ncsa-cert-request
#
# wrapper script that calls ncsa-cert-request.pl with a perl executable
#

if [ "$GLOBUS_LOCATION" = "" ]; then
  echo "Set GLOBUS_LOCATION before running this script"
  exit 1
fi

perl $GLOBUS_LOCATION/bin/ncsa-cert-request.pl $*

